<c:LauncherPage x:Class="StardewLauncher.App.Pages.PageResource"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:c="clr-namespace:StardewLauncher.App.Controls"
                xmlns:v="clr-namespace:StardewLauncher.App.Views">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="18" FontWeight="Bold" Text="资源中心" />
            <TextBlock Margin="0,4,0,0"
                       FontSize="12"
                       Foreground="{DynamicResource Text.Secondary}"
                       Text="下载 Mod、安装 SMAPI，或在内嵌浏览器里直接浏览各 Mod 站点" />
        </StackPanel>

        <!-- 功能分区切换 -->
        <StackPanel Grid.Row="1"
                    Margin="0,14,0,0"
                    Orientation="Horizontal">
            <c:OutlineButton x:Name="BtnTabDownload"
                             Content="下载 Mod"
                             Tag="0"
                             Tone="Solid"
                             Click="OnTabClick" />
            <c:OutlineButton x:Name="BtnTabSmapi"
                             Margin="8,0,0,0"
                             Content="SMAPI"
                             Tag="1"
                             Tone="Outline"
                             Click="OnTabClick" />
            <c:OutlineButton x:Name="BtnTabBrowse"
                             Margin="8,0,0,0"
                             Content="在线浏览"
                             Tag="2"
                             Tone="Outline"
                             Click="OnTabClick" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <v:DownloadCenterView x:Name="ViewDownload" />
            <v:SmapiInstallView x:Name="ViewSmapi" Visibility="Collapsed" />

            <!-- 内嵌浏览器较重，切到该分区时才创建 -->
            <Grid x:Name="PanBrowse" Visibility="Collapsed" />
        </Grid>
    </Grid>
</c:LauncherPage>
