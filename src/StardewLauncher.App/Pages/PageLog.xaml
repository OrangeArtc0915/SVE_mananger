<c:LauncherPage x:Class="StardewLauncher.App.Pages.PageLog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:c="clr-namespace:StardewLauncher.App.Controls">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="18" FontWeight="Bold" Text="运行日志" />
            <TextBlock Margin="0,4,0,0"
                       FontSize="12"
                       Foreground="{DynamicResource Text.Secondary}"
                       Text="启动器的操作记录与游戏进程输出；只留最近 500 条在内存里，切走页面也不会丢" />
        </StackPanel>

        <!-- 子标签 -->
        <StackPanel Grid.Row="1"
                    Margin="0,14,0,0"
                    Orientation="Horizontal">
            <c:OutlineButton x:Name="BtnAppLog"
                             Content="应用日志"
                             Tag="0"
                             Tone="Solid"
                             Click="OnTabClick" />
            <c:OutlineButton x:Name="BtnGameLog"
                             Margin="8,0,0,0"
                             Content="游戏日志"
                             Tag="1"
                             Tone="Outline"
                             Click="OnTabClick" />
        </StackPanel>

        <c:SurfaceCard Grid.Row="2"
                       Margin="0,12,0,0"
                       UseShadow="False"
                       HasHoverEffect="False">
            <Grid>
                <TextBlock x:Name="LabEmpty"
                           VerticalAlignment="Top"
                           FontSize="12"
                           Foreground="{DynamicResource Text.Tertiary}"
                           Text="还没有日志。" />
                <ScrollViewer x:Name="ScrollLog"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              Visibility="Collapsed">
                    <StackPanel x:Name="PanLines" />
                </ScrollViewer>
            </Grid>
        </c:SurfaceCard>
    </Grid>
</c:LauncherPage>
