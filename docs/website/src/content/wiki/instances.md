---
title: 游戏实例
description: 原版与 Mod 端、目录定位、多实例并存、存档位置。
category: 使用
order: 1
---

## 实例是什么

一个「实例」= 一套互不干扰的游戏运行配置。它记录：

- 用哪种方式启动（原版 / SMAPI）
- 用哪个 Mods 目录
- 用哪个存档目录
- 该实例自己的 SMAPI 配置

## 两种类型

| 类型 | 启动 | 说明 |
| --- | --- | --- |
| 原版 | `Stardew Valley.exe` | 不加载任何 Mod，适合纯净存档、排查「是不是 Mod 的锅」 |
| Mod 端 | 经 SMAPI 启动 | Mod 管理、依赖检查等功能只对这个类型的实例生效 |

## 游戏目录怎么找

启动器优先自动定位，顺序大致是：

1. 你在设置里手动指定的目录（优先级最高，一旦指定就不再自动猜）
2. 从 Steam 的安装信息里找：注册表 + `libraryfolders.vdf` 里的库目录

自动定位失败时，到「设置 → 游戏与实例」手动指到游戏根目录 —— 也就是含 `Stardew Valley.exe` 的那一层。

## 多实例并存

可以同时存在一套原版与多套 Mod 端，例如：

- `原版`：纯净体验，存档不动
- `日常 Mod 端`：平时玩的 Mod 组合
- `试验 Mod 端`：想试新 Mod、怕搞坏存档时用这个

实例数据都在 `Data\instances\` 下：`<id>.json` 是配置，`<id>\` 里是该实例的 `Mods\` 与 `Saves\`。也就是说，**每个实例的存档是分开的**。

> 想让实例的 Mod 目录放到别的盘（比如 Mod 很多放 D 盘），在新建实例时可以指定 Mod 库根目录；也可以在设置里改「Mod 库目录」的默认位置。

## 存档

- 游戏自身的存档默认在 `%appdata%\StardewValley\Saves`
- 启动器读取这个路径做展示与备份，**不会主动删改你的存档**
- 如果同时开着 Steam 云同步，建议不要让两个工具同时改写同一份存档

## 切实例

在「游戏实例」页选中某个实例即可，顶部与启动按钮会跟着变。启动前启动器会再校验一次该实例的 Mod 与前置依赖，缺东西会先提示。
