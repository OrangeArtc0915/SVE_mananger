# 构建产物
bin/
obj/
publish/

# 运行时数据：内含设置与可能的密钥（如 Nexus API Key），绝不能进版本库。
# 同样锚定到根目录，避免以后出现同名源码目录时被一起忽略。
/Data/
*.failed

# 放在仓库里的游戏本体副本与 Mod 库，体积很大，不进版本库。
# 这几条必须锚定在仓库根目录（开头的 /）：写成 MODS/ 这种不带锚点的形式，
# 会连 src/StardewLauncher.Core/Mods/ 这样的源码目录一起忽略掉。
/Stardew Valley/
/Stardew Valley SMAPI/
/MODS/
/SMAPI * installer*

# 游戏美术素材：版权归 ConcernedApe，且体积大，不随本仓库分发
星露谷物语素材/

# 参考用的第三方项目：含他人版权（JuniGrid 保留所有权利 / SMUI-2023 GPL-3.0），
# 混入会让他人代码落进本仓库的授权范围，因此不进版本库
JuniGrid/
SMUI-2023/

# 参考用的第三方联机模块：同上，且体积大
HMOL联机模块/

# 另一个独立项目（超级星露谷 / 星露谷助手），与本启动器无关
stardew-valley-assistant/

# 构建工具安装包：.NET SDK 安装器约 200 MB，属于本机环境而非源码
dotnet-sdk-*.exe

# 临时截图
Screenshot_*.jpg
Screenshot_*.png

# 用户级覆盖
*.user
.vs/
.idea/
